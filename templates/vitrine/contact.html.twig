{% extends 'base.html.twig' %}

{% block title %}Contact VitrineController{% endblock %}

{% block body %}
<div>
    <h1>page  contact</h1>
    <div class="row">
        <div class="offset-3 col-6">
            {{ form_start(form) }}
            {{ form_row(form.name, {'attr':{'placeholder':'Nom contact'}}) }}
            {{ form_widget(form) }}
            <div class="mb-3 text-end">
            <button class="btn btn-primary" type="submit" formnovalidate>Valider</button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
